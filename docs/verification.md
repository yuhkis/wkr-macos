# Public v2 の検証

対象候補は **macOS 0.8.0-public.beta.5 / build 45、配列 2.0.0-beta.1、練習 0.1.0**。この候補は整理済みソースから独立した公開履歴を作り、公開文書・ソースコメント、公開前ガードと版表示を更新したものです。beta.2 から変換処理・練習処理・教材は変更していません。以下では、自動検証、過去の候補での実機確認、未確認事項を分けます。

公開記録には再現に必要な製品の条件・期待結果・確認結果だけを載せます。日常利用の時系列、端末の個人設定、実際の打鍵集計、実キーマップ、診断ログの原文は含めません。集計例はテストコードで組み立てた合成データです。公開履歴の確認状況は [publication-audit.md](publication-audit.md) を参照してください。

## 自動検証

2026-09-21、候補ソースに対して実施。以下の自動検証を再実行しました。

| 対象 | 再現条件と期待結果 | 確認結果・限界 |
| --- | --- | --- |
| 公開前ガード | `python3 Scripts/test_publication_guard.py`。合成のGit履歴・ZIPと実際のpre-push hookで拒否条件を再現 | 15件成功。過去履歴の混入、監査後の変更、別URL/ID、未承認pushを拒否。文書の内容確認は別途必要 |
| Swift全体 | `make test`。CoreとmacOS境界のテスト | 256件成功（Core 242、macOS 14）、失敗0件 |
| 配列同期 | 固定した上流JSONから、規則ID・キー・出力・deliveryを照合 | 233規則一致。実IMEでの候補変換は別確認 |
| 教材共有 | 上流の教材と同梱HTML / JS / CSS / data.jsのハッシュを照合 | 12課題・127問。同じ教材を使用 |
| Publicの保存境界 | `make check-public`。CLI・defaults・bridge・教材を許可リストと照合 | 詳細入力ログ・研究計測・任意ページ読み込みの経路を含めないことを検査 |
| 頻度の保存と退避 | 一時ディレクトリと合成バケットで、書き込み失敗・再試行・配列変更時の退避を発生させる | 重複計上を防ぐ。退避に失敗した原ファイルを保持。保存ファイルは0600 |
| 練習成績 | 合成の課題別成績を保存・読み直し・削除 | 許可項目だけを保存。入力本文・誤入力・時刻は保存対象外 |
| エンジン排他 | 独立したleaseインスタンスで同じファイルロックの取得を競合させる | 同時取得を拒否。旧版は共通ロックに対応しないため、先に終了する手順が必要 |
| 同梱練習の回帰 | 一時ディレクトリの別表記を含むresource URLからWKWebViewを開く | 実際のDOMに12課題が構築される。修正前の失敗と修正後の成功を確認 |
| 同梱練習の失敗処理 | 通信遮断ルールの準備失敗、読み込み後のWebContent停止、閉じた後のcallback | 未保護のページを開かない。失敗と再読み込みを案内。閉じた画面を再開しない |

### 合成データの出所

以下は 公開ソースの `Tests/WKRCoreTests/KeyFrequencyTests.swift` がテスト内で生成する値です。個人の集計を匿名化・縮小したものではありません。

| テスト | 生成する入力 | 期待結果 |
| --- | --- | --- |
| `testShiftIsPartOfTheIdentity` | Aを1回、Shift+Aを2回、メモリ内の集計器へ渡す | 別の識別子として1回と2回、総数3回 |
| `testCountsAreFiledUnderTheirOwnDay` | 固定した日Aに1回、日Bに2回 | 日Aは1回、日Bは2回 |
| `testStoreRoundTripPreservesEveryCount` | A・Shift+A・Spaceを合成し、集計モデルを保存形式へ変換して復元 | 3回の内訳が変換前と一致 |
| `testEncodedStoreIsStableAcrossRuns` | 同じ内訳を異なる順で集計器へ渡す | 書き出すJSONが一致し、投入順を持ち込まない |
| `testUnknownSchemaVersionIsDiscarded` | 未対応schemaのJSONをテスト内で作る | 対応している集計として誤解釈しない。原ファイルの保全はmacOSの保存層で別に検査 |

## 実機・画面で確認した範囲

以下は過去のPublic候補の確認結果です。今回の文書整理候補で物理打鍵を再実施したという意味ではありません。

| 対象候補・条件 | 確認できたこと | この確認に含まないこと |
| --- | --- | --- |
| beta.1 / build 41、Public通常起動 | 利用者による権限付与と短い日本語の実変換 | 全規則、変換候補、各アプリの網羅確認 |
| beta.2 / build 42、同梱練習を練習専用起動 | 利用者による課題・配列図の表示確認。読み込み、課題選択、版表示、成績保存オフの応答も確認 | この版でのPublicエンジンによる実変換・採点・一時停止 |
| beta.2 / build 42、releaseアプリを一時ディレクトリへコピー | 12課題、短文への切替、成績保存オフのNative bridge応答 | インストール、ダウンロード後のGatekeeper許可 |
| 同じ教材0.1.0、ブラウザのローカルfile URL | オフライン表示、12課題・127問、合成成績の保存・再読込・削除、合成composition境界、HTTP(S)要求0 | 各ブラウザでの実IMEの確定操作 |
| 同梱練習、隔離した試験用保存先 | 合成の空成績を画面から保存して0600を確認し、画面から削除。閉じた後の練習専用プロセス終了 | 既存利用者の成績や日常設定を使った試験 |

### 白画面の再現条件と修正

一時ディレクトリには同じ場所を指す別表記があります。文書URLだけを正規化し、読み取り許可の基点を正規化しない組み合わせでは、WKWebViewがメイン文書を拒否しました。修正後は教材ディレクトリを一度正規化し、HTMLとread-access範囲を同じ基点から作ります。通信遮断の準備失敗時に制限を外して読み込むことはしません。

## 安全停止の確認条件

再現試験では日常の作業文書や実入力ログを使わず、破棄できるテキスト欄を用います。

| 条件 | 期待する動作 | 現行ベータでの確認範囲 |
| --- | --- | --- |
| Secure Event Inputまたは権限不足 | 変換と集計を止め、内部の保留を破棄する | 状態処理・保存境界は自動検証。今回の候補での実機再現は未実施 |
| 対象外入力ソース・アプリ切替 | 別の入力先へ保留を持ち越さない | Coreの境界処理を自動検証。アプリ別実機確認は未完了 |
| メニューから一時停止・再開 | 停止中は通常入力、再開時は安全条件を再確認する | 利用者による物理打鍵での停止直後の確認は未了 |
| アプリ終了 | 通常入力へ戻る | 練習専用プロセス終了は確認。変換エンジン終了直後の本人の実打鍵は未確認 |

メニューやログに保持プロセス名を載せない設計理由は [design.md](design.md) 7節にあります。試験報告にも実PID・個人の利用アプリ・保持時間の原記録を貼りません。

## 以前の設計確認との関係

v1時点では、TextEdit・Notes・Safariでの基本かな、TextEditでの記号、TextEditとClaude.appでの英字への打ち直しを試験対象として確認しています。これらは特定の入力経路を選ぶ設計根拠であり、Public v2の対応確認へ繰り上げません。

過去の試験から残す制約は、Escapeと英数の読み戻しがアプリや未確定文字列の状態に依存すること、Unicode直接注入を未確定文字列中では抑止すること、TCC許可がビルドのidentityに結び付くことです。期待する処理と制約は [design.md](design.md) 5・8節を参照してください。日常利用の経過時間・利用ソフトの一覧・実打鍵の回数はこの根拠の説明に用いません。

## ビルド・配布

`make package` はcleanなcommitからreleaseアプリを生成し、同梱文書と一緒にzipへ収録します。`build/distribution/<アプリ版>/`へ出力し、manifestにはソースcommit、アプリ・配列・教材の版、SHA256を記録します。今回のreleaseビルド、`--version`のアプリ・配列・教材の表示、ad-hoc署名のdeep / strict検査は成功しました。zip生成後はmanifest・全収録ファイル・展開後の署名を別に照合します。

配布方式は **ad-hoc署名・未公証** です。署名の構造検査と、Developer IDによる発行者確認・Apple公証は別です。以前の候補では署名のdeep / strict検査が成功し、公証チケットはなく、Gatekeeperの既定評価はrejectedでした。証明書の取得・変更を今回の条件にしません。個別起動許可は [install.md](install.md) を参照してください。

## 未確認事項

- beta.5での物理打鍵、変換候補、停止直後・終了直後の通常入力、スリープ復帰。
- 実インストール、権限の取消し、アンインストール、ダウンロードしたzipからの初回起動許可。
- Intel、macOSの全対応版、他のキーボード配列、他ブラウザ・支援技術、長時間運用。
- Google日本語入力・azooKeyのv2テーブルによる実IME入力、仮想マシンとの入力モード同期。
- 既存Private版への共通排他ロック・保存先移行の反映。

初回ベータの実機確認は利用者が選んだ1台での短い確認に限定し、それ以外は未検証として示します。現在の公開保留理由は [公開前監査](publication-audit.md) に記載します。

## 2026-09-21 — 配列v1.1対応の保存ソース

`Legacy/wkr-macos-v1`はWKR macOS 0.7.0を元にした保存ソースです。配列1.1.0と対応し、保存版は0.7.0-archive.1として識別します。旧版の変換Core・規則を保持し、個人観測コメント、詳細な入力状態ログ・打鍵数ログ・前面アプリ識別子ログを省きました。保存版専用の設定・保存先、現行Publicと共通の排他ロック、未対応CLI拒否を追加しています。

旧版のCoreテスト212件と、設定・CLI・ロックの境界テスト4件が成功しました。値・日付・キーマップ・PIDは合成fixtureです。現行Public v2の変換処理・設定・配布済みバイナリはこの追加で変更しません。保存版の実機入力は再検証していません。[保存版README](../Legacy/wkr-macos-v1/README.md)に導入と停止、設定削除、未確認範囲を記載しています。

## 2026-09-22 — 3製品と練習帳0.2.0の配布準備

v1 0.7.1-public.1、v2 0.8.0-public.beta.6、練習帳0.2.0を対象に検証しました。配列1.1.0 / 2.0.0-beta.1は別管理です。

- v1: 226件成功。固定した旧v1規則を使う181件、現在の共通状態表示・日別保存形式41件、製品・設定・CLI・排他境界4件。保存ソース本体はハッシュ照合し、変更していません。
- v2: 257件成功（Core 242、macOS 15）。同梱WKWebViewの12課題表示、通信遮断準備失敗時の案内、閉じた後のcallback、旧成績保全と新成績の0600保存・削除を含みます。
- 公開ガード: 両リポジトリで22件成功。stage済みの個人連絡先、commit本文、許可外identity、私的パス、入れ子ZIP、UTF-16の連絡先、版付き成績ファイルを合成データで拒否しました。
- Web: 配列のPython検査7件、JavaScript検査9件成功。233規則と全教材のキー列を照合。試験用ブラウザで母音9問、自由入力・Backspace、回答の採点、成績保存・再読込・削除、合成IME確定後のEnter境界を確認しました。画面と静的ファイルの取得を確認し、外部リクエストはありませんでした。

これらは自動・合成入力の確認であり、新しいアプリでの本人の物理打鍵や実IME確認を含みません。新バイナリのGatekeeper個別許可、TCC、Apple日本語入力、停止後の通常入力、更新・アンインストールは実機未確認です。既存betaでの確認を新しい版での実測とは扱いません。

### 独立した練習アプリのローカル画面

releaseビルドのWakaraPractice.appで、12課題・配列版・練習版・保存オフの表示を確認しました。ABC入力のHで「あ」が表示され、Enterで採点できました。WKR・IMEモードでは合成の「あ」を貼り付け、Enterで採点できました。標準の編集メニューがないと貼り付けショートカットが働かなかったため、同梱版と独立版の共有画面に編集メニューを追加しました。練習画面の関連テスト3件を再実行し成功しました。終了後の試験用プロセス停止も確認しました。

これはローカル生成アプリの画面確認です。Apple日本語入力による物理打鍵、インターネットから取得したアプリの初回許可、インストール・削除操作とは別です。今回の画面試験で成績保存を有効にしていません。

## 2026-09-22 — 配布候補の画面確認と権限登録の不一致

対象はsource commit `778e4f5530dff4905e3472a121d63a125351e37e`から生成したv1 0.7.1-public.1、v2 0.8.0-public.beta.6、練習帳0.2.0です。macOS 27.0 / Apple Siliconで、同じ3 ZIPのSHA256と展開後のad-hoc署名のdeep / strict検査、各製品の版表示を照合しました。ファイル転送した候補を使い、画面共有のキー操作で合成のテスト文を入力しています。物理キーボードや、ブラウザから取得したファイルのGatekeeper確認とは区別します。

| 条件 | 結果 | 範囲の限界 |
| --- | --- | --- |
| 独立練習帳、ABC、QWERTYモード | Hから「あ」を表示し、Enterで採点 | 物理キーボードの確認ではない |
| v2同梱練習、変換エンジン停止、Apple日本語入力 | aから「あ」を入力。最初のEnterで確定、次のEnterで採点。教材を表示し、白画面にならない | v2の変換エンジンを通した結果ではない |
| v1 / v2、入力権限不足 | 未許可を案内し、変換を開始しない | 許可後の入力・一時停止・再開は未完了 |
| v2、別のWKR変換エンジンが稼働中 | 重複する変換を開始しない | すべての旧版との組み合わせを確認したものではない |

異なるad-hocビルドの許可登録が残っている条件では、設定画面がオンでもv2の実際の権限判定は未許可になりました。OSの署名照合が以前の識別子を参照し、今回の候補と一致していないことを確認しています。Finderからの起動でも再現したため、SSH起動だけの問題とは扱いません。

対象の許可登録を解除した後、遠隔画面からの再許可操作は反映を確認できませんでした。その操作が反映されない原因と、再登録による復旧は未確認です。試験候補を終了し、一時権限を解除して試験を終えました。v1 / v2の許可後のかな変換、Space・Backspace、一時停止・再開は配布前の残件です。練習帳の確認結果を変換エンジンの合格へ読み替えません。

保存はオフで確認しました。成績の保存・削除、更新・アンインストール、スリープ復帰、他IME・Intel・他のOS版は今回の実機確認に含めません。

## 2026-09-22 — v2候補の権限復旧と短い入力・停止・練習確認

対象は上記と同じsource commit `778e4f5530dff4905e3472a121d63a125351e37e`から生成したWKRPublic 0.8.0-public.beta.6 / build 46、配列2.0.0-beta.1、同梱練習0.2.0です。バイナリを変更せず、macOS 27.0 / Apple Silicon、Apple日本語入力のローマ字入力・ひらがな、prefix方式で確認しました。画面共有からのキー操作で合成のテスト文を使っています。

| 条件・操作 | 確認結果 |
| --- | --- |
| 対象候補の入力監視・イベント制御を許可して起動 | アプリ自身が両方を許可済みと判定し、変換を開始。前項で未確認だったv2候補の権限復旧を確認 |
| TextEditでE K、W E RとEnter | 「き」「わから」の入力と確定を確認 |
| Space、候補を戻してEnter、Backspace | IME候補を表示し、確定後に削除できることを確認 |
| メニューで一時停止 | 「一時停止中」「変換を再開」の表示と、通常のローマ字Aから「あ」になることを確認 |
| メニューで再開 | 「変換中」に戻り、E Kから「き」を入力できることを確認 |
| 同梱練習をWKR・IMEモードで開く | 白画面にならず教材を表示。Hから「あ」を入力し、最初のEnterで確定、次のEnterで正答判定。二重変換せず次の問題へ進む案内を表示 |
| メニューから終了 | 変換プロセスの終了と、TextEditで通常のローマ字Aから「あ」を入力・確定できることを確認 |

日別頻度・練習成績の保存はオフで確認しました。この結果はv2候補の確認であり、v1候補の合格とは扱いません。v1は両権限が未許可の場合に変換を開始しないことまで確認しています。

本人の物理打鍵、インターネットから取得したZIPのGatekeeper許可、新規ユーザー、カーソル編集・かな/英数切替の網羅確認、成績の保存・削除、更新・アンインストール、スリープ復帰、Notes・Safari、他IME・Intel・他OS版は今回の確認に含めません。短い確認を全規則・全アプリの互換性保証へ広げません。

## 2026-09-22 — v1候補の許可後の入力・停止確認

対象は同じsource commit `778e4f5530dff4905e3472a121d63a125351e37e`から生成したWKRV1 0.7.1-public.1 / build 12、配列1.1.0です。macOS 27.0 / Apple Silicon、Apple日本語入力のローマ字入力・ひらがな、prefix方式で、画面共有から合成入力を行いました。

| 条件・操作 | 確認結果 |
| --- | --- |
| 両権限を設定して再起動 | 入力監視・イベント制御が許可済みとなり、変換開始。試験後の再起動でも両権限と開始を確認 |
| TextEditでE I | v1の規則どおり「きゅ」を表示。v2のE Iとは区別 |
| Space、Escape、Enter、Backspace | IME候補の表示・取消、確定と末尾文字の削除を確認 |
| 一時停止 | メニューが「一時停止中」「変換を再開」に変わり、通常のローマ字Aから「あ」を入力・確定 |
| 再開 | E Iから再び「きゅ」を入力・確定 |
| メニューから終了 | 変換プロセスが終了し、通常のローマ字Aから「あ」を入力・確定 |

権限設定直後の別セッションで、入力監視のタイムアウトを受けて安全終了する事象を1回確認しました。再起動後の上記セッションでは、入力からメニュー終了までタイムアウトは再発しませんでした。安全終了の直接の契機は確認できていますが、タイムアウトの根本原因と長時間利用時の再現性は未特定です。

日別頻度の保存はオフで確認しました。前項と同じく、物理打鍵、ダウンロード後のGatekeeper許可、新規ユーザー、他アプリ・他IME・他OS、更新・アンインストール、長時間利用の確認を含みません。今回の結果によりv1の短い入力・停止の確認は完了しましたが、全環境での動作保証とは扱いません。
